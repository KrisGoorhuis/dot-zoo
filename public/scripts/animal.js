class Animal {
   constructor(x, y) {
      this.color = "blue";
      this.x = x; // + / - the left or right bounds?
      this.y = y; // +/- the top or bottom bounds?
      this.vectorX = 0;
      this.vectorY = 0;
      this.radius = 10; 
      this.movementFrequency = 2000;
      this.movementSpeed = 7; // Inversely proportional - milliseconds between position incrementation
   }
}





